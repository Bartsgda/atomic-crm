# MEMORY INDEX

- [CRM-Atomic typecheck validation](feedback_crm_atomic_typecheck.md) — `npx tsc` = no-op EXIT 0; realny check `tsc -p tsconfig.app.json` ma ~414-błędowy baseline → waliduj diffem, nie absolutnym EXIT.
